<simplic:DefaultRibbonWindow x:Class="Simplic.Boilerplate.UI.ContactWindow"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                             xmlns:simplic="http://schemas.simplic-systems.com/2016/xaml/presentation"
                             xmlns:local="clr-namespace:Simplic.Boilerplate.UI"
                             mc:Ignorable="d" 
                             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <simplic:TextBox Text="{Binding Name}" MinWidth="150" Grid.Row="0"></simplic:TextBox>
        <simplic:TextBox Text="" MinWidth="150" Grid.Row="1"></simplic:TextBox>
        <ListBox ItemsSource="{Binding Others}" Grid.Row="2"></ListBox>
    </Grid>
</simplic:DefaultRibbonWindow>
